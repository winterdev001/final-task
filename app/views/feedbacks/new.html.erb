<body class="bg-main ">
    <div class="d-flex justify-content-center job-show">
        <p id="notice"><%= notice %></p>
        <div class="card mb-3" style="width:30rem;">
            <h2 class="card-header text-center">New Feedback</h2>
            <div class="card-body">  
                <%= render 'form', feedback: @feedback %>

                <%= link_to 'Back', companies_path, class:'btn btn-light' %>
            </div>
        </div>
    </div>  
</body>
